<%# project = Project.find(@project.id) %>
<% contract =  Contract.find_by project_id: @project%>
<% if contract.present? %>
<%= link_to l(:label_contract_index), edit_contract_path(project_id: @project) %>
<% end %>
